services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/srv/data   # задачи, логи, кэш модели whisper
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2g
